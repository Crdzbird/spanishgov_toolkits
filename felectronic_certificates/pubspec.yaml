name: felectronic_certificates
description: >-
  Flutter plugin for managing device-stored certificates (import, sign, list,
  delete) on Android and iOS using KeyStore and Keychain.
version: 1.0.0
repository: https://github.com/Crdzbird/spanishgov_toolkits
issue_tracker: https://github.com/Crdzbird/spanishgov_toolkits/issues
topics:
  - certificate
  - pkcs12
  - signing
  - keystore
  - keychain

environment:
  flutter: ">=3.22.0"
  sdk: ^3.11.0

# Part of the pub workspace declared in the repository root pubspec.yaml.
resolution: workspace

flutter:
  plugin:
    platforms:
      android:
        default_package: felectronic_certificates_android
      ios:
        default_package: felectronic_certificates_ios

dependencies:
  felectronic_certificates_android: ^1.0.0
  felectronic_certificates_ios: ^1.0.0
  felectronic_certificates_platform_interface: ^1.0.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  plugin_platform_interface: ^2.1.8
  very_good_analysis: ^10.2.0
