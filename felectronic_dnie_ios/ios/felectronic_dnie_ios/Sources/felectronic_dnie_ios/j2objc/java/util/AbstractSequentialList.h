//
//  Generated by the J2ObjC translator.  DO NOT EDIT!
//  source: android/platform/libcore/ojluni/src/main/java/java/util/AbstractSequentialList.java
//

#include "J2ObjC_header.h"

#pragma push_macro("INCLUDE_ALL_JavaUtilAbstractSequentialList")
#ifdef RESTRICT_JavaUtilAbstractSequentialList
#define INCLUDE_ALL_JavaUtilAbstractSequentialList 0
#else
#define INCLUDE_ALL_JavaUtilAbstractSequentialList 1
#endif
#undef RESTRICT_JavaUtilAbstractSequentialList

#if __has_feature(nullability)
#pragma clang diagnostic push
#pragma GCC diagnostic ignored "-Wnullability"
#pragma GCC diagnostic ignored "-Wnullability-completeness"
#endif

#if !defined (JavaUtilAbstractSequentialList_) && (INCLUDE_ALL_JavaUtilAbstractSequentialList || defined(INCLUDE_JavaUtilAbstractSequentialList))
#define JavaUtilAbstractSequentialList_

#define RESTRICT_JavaUtilAbstractList 1
#define INCLUDE_JavaUtilAbstractList 1
#include "java/util/AbstractList.h"

@class JavaLangBoolean;
@class JavaLangInteger;
@protocol JavaUtilCollection;
@protocol JavaUtilIterator;
@protocol JavaUtilListIterator;

/*!
 @brief This class provides a skeletal implementation of the <code>List</code>
  interface to minimize the effort required to implement this interface
  backed by a "sequential access" data store (such as a linked list).For
  random access data (such as an array), <code>AbstractList</code> should be used
  in preference to this class.
 <p>
  This class is the opposite of the <code>AbstractList</code> class in the sense
  that it implements the "random access" methods (<code>get(int index)</code>,
  <code>set(int index, E element)</code>, <code>add(int index, E element)</code> and 
 <code>remove(int index)</code>) on top of the list's list iterator, instead of
  the other way around.<p>
  To implement a list the programmer needs only to extend this class and
  provide implementations for the <code>listIterator</code> and <code>size</code>
  methods.  For an unmodifiable list, the programmer need only implement the
  list iterator's <code>hasNext</code>, <code>next</code>, <code>hasPrevious</code>,
  <code>previous</code> and <code>index</code> methods.<p>
  For a modifiable list the programmer should additionally implement the list
  iterator's <code>set</code> method.  For a variable-size list the programmer
  should additionally implement the list iterator's <code>remove</code> and 
 <code>add</code> methods.<p>
  The programmer should generally provide a void (no argument) and collection
  constructor, as per the recommendation in the <code>Collection</code> interface
  specification.<p>
  This class is a member of the 
 <a href="{@@docRoot}/java.base/java/util/package-summary.html#CollectionsFramework">
  Java Collections Framework</a>.
 @author Josh Bloch
 @author Neal Gafter
 - seealso: Collection
 - seealso: List
 - seealso: AbstractList
 - seealso: AbstractCollection
 @since 1.2
 */
@interface JavaUtilAbstractSequentialList : JavaUtilAbstractList

#pragma mark Public

/*!
 @brief Inserts the specified element at the specified position in this list
  (optional operation).Shifts the element currently at that position
  (if any) and any subsequent elements to the right (adds one to their
  indices).
 <p>This implementation first gets a list iterator pointing to the
  indexed element (with <code>listIterator(index)</code>).  Then, it
  inserts the specified element with <code>ListIterator.add</code>.
  
 <p>Note that this implementation will throw an 
 <code>UnsupportedOperationException</code> if the list iterator does not
  implement the <code>add</code> operation.
 @throw UnsupportedOperationException
 @throw ClassCastException
 @throw NullPointerException
 @throw IllegalArgumentException
 @throw IndexOutOfBoundsException
 */
- (void)addWithInt:(jint)index
            withId:(id)element;

/*!
 @brief Inserts all of the elements in the specified collection into this
  list at the specified position (optional operation).Shifts the
  element currently at that position (if any) and any subsequent
  elements to the right (increases their indices).
 The new elements
  will appear in this list in the order that they are returned by the
  specified collection's iterator.  The behavior of this operation is
  undefined if the specified collection is modified while the
  operation is in progress.  (Note that this will occur if the specified
  collection is this list, and it's nonempty.) 
 <p>This implementation gets an iterator over the specified collection and
  a list iterator over this list pointing to the indexed element (with 
 <code>listIterator(index)</code>).  Then, it iterates over the specified
  collection, inserting the elements obtained from the iterator into this
  list, one at a time, using <code>ListIterator.add</code> followed by 
 <code>ListIterator.next</code> (to skip over the added element). 
 <p>Note that this implementation will throw an 
 <code>UnsupportedOperationException</code> if the list iterator returned by the 
 <code>listIterator</code> method does not implement the <code>add</code>
  operation.
 @throw UnsupportedOperationException
 @throw ClassCastException
 @throw NullPointerException
 @throw IllegalArgumentException
 @throw IndexOutOfBoundsException
 */
- (jboolean)addAllWithInt:(jint)index
   withJavaUtilCollection:(id<JavaUtilCollection>)c;

/*!
 @brief Returns the element at the specified position in this list.
 <p>This implementation first gets a list iterator pointing to the
  indexed element (with <code>listIterator(index)</code>).  Then, it gets
  the element using <code>ListIterator.next</code> and returns it.
 @throw IndexOutOfBoundsException
 */
- (id)getWithInt:(jint)index;

/*!
 @brief Returns an iterator over the elements in this list (in proper
  sequence).
 <p>
  This implementation merely returns a list iterator over the list.
 @return an iterator over the elements in this list (in proper sequence)
 */
- (id<JavaUtilIterator> __nonnull)iterator;

/*!
 @brief Returns a list iterator over the elements in this list (in proper
  sequence).
 @param index index of first element to be returned from the list          iterator (by a call to the 
 <code>next</code>  method)
 @return a list iterator over the elements in this list (in proper
          sequence)
 @throw IndexOutOfBoundsException
 */
- (id<JavaUtilListIterator> __nonnull)listIteratorWithInt:(jint)index;

/*!
 @brief Removes the element at the specified position in this list (optional
  operation).Shifts any subsequent elements to the left (subtracts one
  from their indices).
 Returns the element that was removed from the
  list. 
 <p>This implementation first gets a list iterator pointing to the
  indexed element (with <code>listIterator(index)</code>).  Then, it removes
  the element with <code>ListIterator.remove</code>.
  
 <p>Note that this implementation will throw an 
 <code>UnsupportedOperationException</code> if the list iterator does not
  implement the <code>remove</code> operation.
 @throw UnsupportedOperationException
 @throw IndexOutOfBoundsException
 */
- (id)removeWithInt:(jint)index;

/*!
 @brief Replaces the element at the specified position in this list with the
  specified element (optional operation).
 <p>This implementation first gets a list iterator pointing to the
  indexed element (with <code>listIterator(index)</code>).  Then, it gets
  the current element using <code>ListIterator.next</code> and replaces it
  with <code>ListIterator.set</code>.
  
 <p>Note that this implementation will throw an 
 <code>UnsupportedOperationException</code> if the list iterator does not
  implement the <code>set</code> operation.
 @throw UnsupportedOperationException
 @throw ClassCastException
 @throw NullPointerException
 @throw IllegalArgumentException
 @throw IndexOutOfBoundsException
 */
- (id)setWithInt:(jint)index
          withId:(id)element;

#pragma mark Protected

/*!
 @brief Sole constructor.
 (For invocation by subclass constructors, typically
  implicit.)
 */
- (instancetype __nonnull)init;

#pragma mark Package-Private

@end

J2OBJC_EMPTY_STATIC_INIT(JavaUtilAbstractSequentialList)

FOUNDATION_EXPORT void JavaUtilAbstractSequentialList_init(JavaUtilAbstractSequentialList *self);

J2OBJC_TYPE_LITERAL_HEADER(JavaUtilAbstractSequentialList)

#endif


#if __has_feature(nullability)
#pragma clang diagnostic pop
#endif
#pragma pop_macro("INCLUDE_ALL_JavaUtilAbstractSequentialList")
