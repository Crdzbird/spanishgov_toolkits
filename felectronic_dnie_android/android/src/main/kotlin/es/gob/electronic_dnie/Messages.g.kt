// Autogenerated from Pigeon (v25.5.0), do not edit directly.
// See also: https://pub.dev/packages/pigeon
@file:Suppress("UNCHECKED_CAST", "ArrayInDataClass")

package es.gob.electronic_dnie

import android.util.Log
import io.flutter.plugin.common.BasicMessageChannel
import io.flutter.plugin.common.BinaryMessenger
import io.flutter.plugin.common.EventChannel
import io.flutter.plugin.common.MessageCodec
import io.flutter.plugin.common.StandardMethodCodec
import io.flutter.plugin.common.StandardMessageCodec
import java.io.ByteArrayOutputStream
import java.nio.ByteBuffer
private object MessagesPigeonUtils {

  fun wrapResult(result: Any?): List<Any?> {
    return listOf(result)
  }

  fun wrapError(exception: Throwable): List<Any?> {
    return if (exception is FlutterError) {
      listOf(
        exception.code,
        exception.message,
        exception.details
      )
    } else {
      listOf(
        exception.javaClass.simpleName,
        exception.toString(),
        "Cause: " + exception.cause + ", Stacktrace: " + Log.getStackTraceString(exception)
      )
    }
  }
  fun deepEquals(a: Any?, b: Any?): Boolean {
    if (a is ByteArray && b is ByteArray) {
        return a.contentEquals(b)
    }
    if (a is IntArray && b is IntArray) {
        return a.contentEquals(b)
    }
    if (a is LongArray && b is LongArray) {
        return a.contentEquals(b)
    }
    if (a is DoubleArray && b is DoubleArray) {
        return a.contentEquals(b)
    }
    if (a is Array<*> && b is Array<*>) {
      return a.size == b.size &&
          a.indices.all{ deepEquals(a[it], b[it]) }
    }
    if (a is List<*> && b is List<*>) {
      return a.size == b.size &&
          a.indices.all{ deepEquals(a[it], b[it]) }
    }
    if (a is Map<*, *> && b is Map<*, *>) {
      return a.size == b.size && a.all {
          (b as Map<Any?, Any?>).containsKey(it.key) &&
          deepEquals(it.value, b[it.key])
      }
    }
    return a == b
  }
      
}

/**
 * Error class for passing custom error details to Flutter via a thrown PlatformException.
 * @property code The error code.
 * @property message The error message.
 * @property details The error details. Must be a datatype supported by the api codec.
 */
class FlutterError (
  val code: String,
  override val message: String? = null,
  val details: Any? = null
) : Throwable()

/**
 * Result of a successful DNIe signing or certificate-reading operation.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class DnieSignedDataMessage (
  val signedData: ByteArray,
  val signedDataBase64: String,
  val certificate: String
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): DnieSignedDataMessage {
      val signedData = pigeonVar_list[0] as ByteArray
      val signedDataBase64 = pigeonVar_list[1] as String
      val certificate = pigeonVar_list[2] as String
      return DnieSignedDataMessage(signedData, signedDataBase64, certificate)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      signedData,
      signedDataBase64,
      certificate,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other !is DnieSignedDataMessage) {
      return false
    }
    if (this === other) {
      return true
    }
    return MessagesPigeonUtils.deepEquals(toList(), other.toList())  }

  override fun hashCode(): Int = toList().hashCode()
}

/**
 * Result of a card probe operation (no PIN required).
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class DnieCardProbeMessage (
  /** Whether the detected card is a valid Spanish DNIe. */
  val isValidDnie: Boolean,
  /** Historical bytes from the card as a hex string. */
  val atrHex: String,
  /** Tag UID / identifier as a hex string. */
  val tagId: String
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): DnieCardProbeMessage {
      val isValidDnie = pigeonVar_list[0] as Boolean
      val atrHex = pigeonVar_list[1] as String
      val tagId = pigeonVar_list[2] as String
      return DnieCardProbeMessage(isValidDnie, atrHex, tagId)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      isValidDnie,
      atrHex,
      tagId,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other !is DnieCardProbeMessage) {
      return false
    }
    if (this === other) {
      return true
    }
    return MessagesPigeonUtils.deepEquals(toList(), other.toList())  }

  override fun hashCode(): Int = toList().hashCode()
}

/**
 * Parsed X.509 certificate details from the DNIe.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class DnieCertificateDetailsMessage (
  /** Subject common name (e.g. "APELLIDO1 APELLIDO2, NOMBRE (FIRMA)"). */
  val subjectCommonName: String,
  /** Subject serial number (NIF, e.g. "12345678Z"). */
  val subjectSerialNumber: String,
  /** Issuer common name. */
  val issuerCommonName: String,
  /** Issuer organization. */
  val issuerOrganization: String,
  /** Certificate validity start as epoch milliseconds. */
  val notValidBefore: Long,
  /** Certificate validity end as epoch milliseconds. */
  val notValidAfter: Long,
  /** Certificate serial number as hex string. */
  val serialNumber: String,
  /** Whether the certificate is currently valid. */
  val isCurrentlyValid: Boolean
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): DnieCertificateDetailsMessage {
      val subjectCommonName = pigeonVar_list[0] as String
      val subjectSerialNumber = pigeonVar_list[1] as String
      val issuerCommonName = pigeonVar_list[2] as String
      val issuerOrganization = pigeonVar_list[3] as String
      val notValidBefore = pigeonVar_list[4] as Long
      val notValidAfter = pigeonVar_list[5] as Long
      val serialNumber = pigeonVar_list[6] as String
      val isCurrentlyValid = pigeonVar_list[7] as Boolean
      return DnieCertificateDetailsMessage(subjectCommonName, subjectSerialNumber, issuerCommonName, issuerOrganization, notValidBefore, notValidAfter, serialNumber, isCurrentlyValid)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      subjectCommonName,
      subjectSerialNumber,
      issuerCommonName,
      issuerOrganization,
      notValidBefore,
      notValidAfter,
      serialNumber,
      isCurrentlyValid,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other !is DnieCertificateDetailsMessage) {
      return false
    }
    if (this === other) {
      return true
    }
    return MessagesPigeonUtils.deepEquals(toList(), other.toList())  }

  override fun hashCode(): Int = toList().hashCode()
}

/**
 * Personal data extracted from the DNIe certificate subject DN.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class DniePersonalDataMessage (
  /** Full name (given name + surnames). */
  val fullName: String,
  /** Given name / first name. */
  val givenName: String,
  /** Surnames (first + second). */
  val surnames: String,
  /** NIF (Numero de Identificacion Fiscal). */
  val nif: String,
  /** Country code (e.g. "ES"). */
  val country: String,
  /** Certificate type (e.g. "FIRMA", "AUTENTICACION"). */
  val certificateType: String
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): DniePersonalDataMessage {
      val fullName = pigeonVar_list[0] as String
      val givenName = pigeonVar_list[1] as String
      val surnames = pigeonVar_list[2] as String
      val nif = pigeonVar_list[3] as String
      val country = pigeonVar_list[4] as String
      val certificateType = pigeonVar_list[5] as String
      return DniePersonalDataMessage(fullName, givenName, surnames, nif, country, certificateType)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      fullName,
      givenName,
      surnames,
      nif,
      country,
      certificateType,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other !is DniePersonalDataMessage) {
      return false
    }
    if (this === other) {
      return true
    }
    return MessagesPigeonUtils.deepEquals(toList(), other.toList())  }

  override fun hashCode(): Int = toList().hashCode()
}

/**
 * NFC hardware status of the device.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class DnieNfcStatusMessage (
  /** Whether the device has NFC hardware. */
  val isAvailable: Boolean,
  /** Whether NFC is enabled in system settings. */
  val isEnabled: Boolean
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): DnieNfcStatusMessage {
      val isAvailable = pigeonVar_list[0] as Boolean
      val isEnabled = pigeonVar_list[1] as Boolean
      return DnieNfcStatusMessage(isAvailable, isEnabled)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      isAvailable,
      isEnabled,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other !is DnieNfcStatusMessage) {
      return false
    }
    if (this === other) {
      return true
    }
    return MessagesPigeonUtils.deepEquals(toList(), other.toList())  }

  override fun hashCode(): Int = toList().hashCode()
}
private open class MessagesPigeonCodec : StandardMessageCodec() {
  override fun readValueOfType(type: Byte, buffer: ByteBuffer): Any? {
    return when (type) {
      129.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          DnieSignedDataMessage.fromList(it)
        }
      }
      130.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          DnieCardProbeMessage.fromList(it)
        }
      }
      131.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          DnieCertificateDetailsMessage.fromList(it)
        }
      }
      132.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          DniePersonalDataMessage.fromList(it)
        }
      }
      133.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          DnieNfcStatusMessage.fromList(it)
        }
      }
      else -> super.readValueOfType(type, buffer)
    }
  }
  override fun writeValue(stream: ByteArrayOutputStream, value: Any?)   {
    when (value) {
      is DnieSignedDataMessage -> {
        stream.write(129)
        writeValue(stream, value.toList())
      }
      is DnieCardProbeMessage -> {
        stream.write(130)
        writeValue(stream, value.toList())
      }
      is DnieCertificateDetailsMessage -> {
        stream.write(131)
        writeValue(stream, value.toList())
      }
      is DniePersonalDataMessage -> {
        stream.write(132)
        writeValue(stream, value.toList())
      }
      is DnieNfcStatusMessage -> {
        stream.write(133)
        writeValue(stream, value.toList())
      }
      else -> super.writeValue(stream, value)
    }
  }
}


/**
 * Host API for DNIe NFC operations, implemented on Android (Kotlin)
 * and iOS (Swift).
 *
 * Generated interface from Pigeon that represents a handler of messages from Flutter.
 */
interface FelectronicDnieHostApi {
  /**
   * Signs [data] using the DNIe private key via NFC.
   *
   * [certificateType] selects which certificate to use: `'SIGN'` or `'AUTH'`.
   */
  fun sign(data: ByteArray, can: String, pin: String, timeout: Long, certificateType: String, callback: (Result<DnieSignedDataMessage>) -> Unit)
  /** Stops an in-progress NFC signing operation. */
  fun stopSign(callback: (Result<Unit>) -> Unit)
  /**
   * Reads the certificate from the DNIe without signing data.
   *
   * [certificateType] selects which certificate to read: `'SIGN'` or `'AUTH'`.
   */
  fun readCertificate(can: String, pin: String, timeout: Long, certificateType: String, callback: (Result<DnieSignedDataMessage>) -> Unit)
  /**
   * Probes an NFC card to check if it is a valid DNIe.
   * No CAN or PIN required.
   */
  fun probeCard(timeout: Long, callback: (Result<DnieCardProbeMessage>) -> Unit)
  /**
   * Reads and parses X.509 certificate details from the DNIe.
   *
   * [certificateType] selects which certificate: `'SIGN'` or `'AUTH'`.
   */
  fun readCertificateDetails(can: String, pin: String, timeout: Long, certificateType: String, callback: (Result<DnieCertificateDetailsMessage>) -> Unit)
  /**
   * Reads personal data from the DNIe certificate subject DN.
   *
   * [certificateType] selects which certificate: `'SIGN'` or `'AUTH'`.
   */
  fun readPersonalData(can: String, pin: String, timeout: Long, certificateType: String, callback: (Result<DniePersonalDataMessage>) -> Unit)
  /**
   * Verifies PIN and CAN credentials without signing.
   *
   * [certificateType] selects which certificate: `'SIGN'` or `'AUTH'`.
   */
  fun verifyPin(can: String, pin: String, timeout: Long, certificateType: String, callback: (Result<Unit>) -> Unit)
  /** Checks if NFC hardware is available and enabled. */
  fun checkNfcAvailability(callback: (Result<DnieNfcStatusMessage>) -> Unit)

  companion object {
    /** The codec used by FelectronicDnieHostApi. */
    val codec: MessageCodec<Any?> by lazy {
      MessagesPigeonCodec()
    }
    /** Sets up an instance of `FelectronicDnieHostApi` to handle messages through the `binaryMessenger`. */
    @JvmOverloads
    fun setUp(binaryMessenger: BinaryMessenger, api: FelectronicDnieHostApi?, messageChannelSuffix: String = "") {
      val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.felectronic_dnie_platform_interface.FelectronicDnieHostApi.sign$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val dataArg = args[0] as ByteArray
            val canArg = args[1] as String
            val pinArg = args[2] as String
            val timeoutArg = args[3] as Long
            val certificateTypeArg = args[4] as String
            api.sign(dataArg, canArg, pinArg, timeoutArg, certificateTypeArg) { result: Result<DnieSignedDataMessage> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(MessagesPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.felectronic_dnie_platform_interface.FelectronicDnieHostApi.stopSign$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.stopSign{ result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesPigeonUtils.wrapError(error))
              } else {
                reply.reply(MessagesPigeonUtils.wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.felectronic_dnie_platform_interface.FelectronicDnieHostApi.readCertificate$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val canArg = args[0] as String
            val pinArg = args[1] as String
            val timeoutArg = args[2] as Long
            val certificateTypeArg = args[3] as String
            api.readCertificate(canArg, pinArg, timeoutArg, certificateTypeArg) { result: Result<DnieSignedDataMessage> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(MessagesPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.felectronic_dnie_platform_interface.FelectronicDnieHostApi.probeCard$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val timeoutArg = args[0] as Long
            api.probeCard(timeoutArg) { result: Result<DnieCardProbeMessage> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(MessagesPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.felectronic_dnie_platform_interface.FelectronicDnieHostApi.readCertificateDetails$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val canArg = args[0] as String
            val pinArg = args[1] as String
            val timeoutArg = args[2] as Long
            val certificateTypeArg = args[3] as String
            api.readCertificateDetails(canArg, pinArg, timeoutArg, certificateTypeArg) { result: Result<DnieCertificateDetailsMessage> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(MessagesPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.felectronic_dnie_platform_interface.FelectronicDnieHostApi.readPersonalData$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val canArg = args[0] as String
            val pinArg = args[1] as String
            val timeoutArg = args[2] as Long
            val certificateTypeArg = args[3] as String
            api.readPersonalData(canArg, pinArg, timeoutArg, certificateTypeArg) { result: Result<DniePersonalDataMessage> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(MessagesPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.felectronic_dnie_platform_interface.FelectronicDnieHostApi.verifyPin$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val canArg = args[0] as String
            val pinArg = args[1] as String
            val timeoutArg = args[2] as Long
            val certificateTypeArg = args[3] as String
            api.verifyPin(canArg, pinArg, timeoutArg, certificateTypeArg) { result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesPigeonUtils.wrapError(error))
              } else {
                reply.reply(MessagesPigeonUtils.wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.felectronic_dnie_platform_interface.FelectronicDnieHostApi.checkNfcAvailability$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.checkNfcAvailability{ result: Result<DnieNfcStatusMessage> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(MessagesPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
    }
  }
}
