// Autogenerated from Pigeon (v26.3.3), do not edit directly.
// See also: https://pub.dev/packages/pigeon
@file:Suppress("UNCHECKED_CAST", "ArrayInDataClass")

package es.gob.electronic_certificates

import android.util.Log
import io.flutter.plugin.common.BasicMessageChannel
import io.flutter.plugin.common.BinaryMessenger
import io.flutter.plugin.common.EventChannel
import io.flutter.plugin.common.MessageCodec
import io.flutter.plugin.common.StandardMethodCodec
import io.flutter.plugin.common.StandardMessageCodec
import java.io.ByteArrayOutputStream
import java.nio.ByteBuffer
private object MessagesPigeonUtils {

  fun wrapResult(result: Any?): List<Any?> {
    return listOf(result)
  }

  fun wrapError(exception: Throwable): List<Any?> {
    return if (exception is FlutterError) {
      listOf(
        exception.code,
        exception.message,
        exception.details
      )
    } else {
      listOf(
        exception.javaClass.simpleName,
        exception.toString(),
        "Cause: " + exception.cause + ", Stacktrace: " + Log.getStackTraceString(exception)
      )
    }
  }
  fun doubleEquals(a: Double, b: Double): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0) 0.0 else a) == (if (b == 0.0) 0.0 else b) || (a.isNaN() && b.isNaN())
  }

  fun floatEquals(a: Float, b: Float): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0f) 0.0f else a) == (if (b == 0.0f) 0.0f else b) || (a.isNaN() && b.isNaN())
  }

  fun doubleHash(d: Double): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (d == 0.0) 0.0 else d
    val bits = java.lang.Double.doubleToLongBits(normalized)
    return (bits xor (bits ushr 32)).toInt()
  }

  fun floatHash(f: Float): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (f == 0.0f) 0.0f else f
    return java.lang.Float.floatToIntBits(normalized)
  }

  fun deepEquals(a: Any?, b: Any?): Boolean {
    if (a === b) {
      return true
    }
    if (a == null || b == null) {
      return false
    }
    if (a is ByteArray && b is ByteArray) {
      return a.contentEquals(b)
    }
    if (a is IntArray && b is IntArray) {
      return a.contentEquals(b)
    }
    if (a is LongArray && b is LongArray) {
      return a.contentEquals(b)
    }
    if (a is DoubleArray && b is DoubleArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!doubleEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is FloatArray && b is FloatArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!floatEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is Array<*> && b is Array<*>) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!deepEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is List<*> && b is List<*>) {
      if (a.size != b.size) return false
      val iterA = a.iterator()
      val iterB = b.iterator()
      while (iterA.hasNext() && iterB.hasNext()) {
        if (!deepEquals(iterA.next(), iterB.next())) return false
      }
      return true
    }
    if (a is Map<*, *> && b is Map<*, *>) {
      if (a.size != b.size) return false
      for (entry in a) {
        val key = entry.key
        var found = false
        for (bEntry in b) {
          if (deepEquals(key, bEntry.key)) {
            if (deepEquals(entry.value, bEntry.value)) {
              found = true
              break
            } else {
              return false
            }
          }
        }
        if (!found) return false
      }
      return true
    }
    if (a is Double && b is Double) {
      return doubleEquals(a, b)
    }
    if (a is Float && b is Float) {
      return floatEquals(a, b)
    }
    return a == b
  }

  fun deepHash(value: Any?): Int {
    return when (value) {
      null -> 0
      is ByteArray -> value.contentHashCode()
      is IntArray -> value.contentHashCode()
      is LongArray -> value.contentHashCode()
      is DoubleArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + doubleHash(item)
        }
        result
      }
      is FloatArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + floatHash(item)
        }
        result
      }
      is Array<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is List<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is Map<*, *> -> {
        var result = 0
        for (entry in value) {
          result += ((deepHash(entry.key) * 31) xor deepHash(entry.value))
        }
        result
      }
      is Double -> doubleHash(value)
      is Float -> floatHash(value)
      else -> value.hashCode()
    }
  }

}

/**
 * Error class for passing custom error details to Flutter via a thrown PlatformException.
 * @property code The error code.
 * @property message The error message.
 * @property details The error details. Must be a datatype supported by the api codec.
 */
class FlutterError (
  val code: String,
  override val message: String? = null,
  val details: Any? = null
) : Throwable()

/**
 * A device-stored certificate transferred between native and Dart layers.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class DeviceCertificateMessage (
  /** Certificate serial number (hex string). */
  val serialNumber: String,
  /** Optional alias / label assigned to the certificate. */
  val alias: String? = null,
  /** Holder (subject) common name. */
  val holderName: String,
  /** Issuer common name. */
  val issuerName: String,
  /** Expiration date formatted as dd-MM-yyyy. */
  val expirationDate: String,
  /** Semicolon-separated key usages, e.g. "SIGNING;AUTHENTICATION". */
  val usages: String,
  /** DER-encoded certificate bytes. */
  val encoded: ByteArray
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): DeviceCertificateMessage {
      val serialNumber = pigeonVar_list[0] as String
      val alias = pigeonVar_list[1] as String?
      val holderName = pigeonVar_list[2] as String
      val issuerName = pigeonVar_list[3] as String
      val expirationDate = pigeonVar_list[4] as String
      val usages = pigeonVar_list[5] as String
      val encoded = pigeonVar_list[6] as ByteArray
      return DeviceCertificateMessage(serialNumber, alias, holderName, issuerName, expirationDate, usages, encoded)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      serialNumber,
      alias,
      holderName,
      issuerName,
      expirationDate,
      usages,
      encoded,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as DeviceCertificateMessage
    return MessagesPigeonUtils.deepEquals(this.serialNumber, other.serialNumber) && MessagesPigeonUtils.deepEquals(this.alias, other.alias) && MessagesPigeonUtils.deepEquals(this.holderName, other.holderName) && MessagesPigeonUtils.deepEquals(this.issuerName, other.issuerName) && MessagesPigeonUtils.deepEquals(this.expirationDate, other.expirationDate) && MessagesPigeonUtils.deepEquals(this.usages, other.usages) && MessagesPigeonUtils.deepEquals(this.encoded, other.encoded)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + MessagesPigeonUtils.deepHash(this.serialNumber)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.alias)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.holderName)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.issuerName)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.expirationDate)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.usages)
    result = 31 * result + MessagesPigeonUtils.deepHash(this.encoded)
    return result
  }
}
private open class MessagesPigeonCodec : StandardMessageCodec() {
  override fun readValueOfType(type: Byte, buffer: ByteBuffer): Any? {
    return when (type) {
      129.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          DeviceCertificateMessage.fromList(it)
        }
      }
      else -> super.readValueOfType(type, buffer)
    }
  }
  override fun writeValue(stream: ByteArrayOutputStream, value: Any?)   {
    when (value) {
      is DeviceCertificateMessage -> {
        stream.write(129)
        writeValue(stream, value.toList())
      }
      else -> super.writeValue(stream, value)
    }
  }
}


/**
 * Host API for device-stored certificate operations, implemented
 * on Android (Kotlin) and iOS (Swift).
 *
 * Generated interface from Pigeon that represents a handler of messages from Flutter.
 */
interface FelectronicCertificatesHostApi {
  /** Returns all certificates stored on the device. */
  fun getAllCertificates(callback: (Result<List<DeviceCertificateMessage?>>) -> Unit)
  /** Returns the currently selected default certificate, if any. */
  fun getDefaultCertificate(callback: (Result<DeviceCertificateMessage?>) -> Unit)
  /** Opens a native certificate picker and returns the selected certificate. */
  fun selectDefaultCertificate(callback: (Result<DeviceCertificateMessage?>) -> Unit)
  /** Sets the default certificate by its serial number. */
  fun setDefaultCertificateBySerialNumber(serialNumber: String, callback: (Result<Unit>) -> Unit)
  /** Clears the default certificate selection. */
  fun clearDefaultCertificate(callback: (Result<Unit>) -> Unit)
  /**
   * Signs [data] using the default certificate's private key.
   *
   * [algorithm] is one of: SHA256RSA, SHA384RSA, SHA512RSA,
   * SHA256EC, SHA384EC, SHA512EC.
   */
  fun signWithDefaultCertificate(data: ByteArray, algorithm: String, callback: (Result<ByteArray>) -> Unit)
  /** Imports a PKCS#12 (.p12/.pfx) file into the device keystore. */
  fun importCertificate(pkcs12Data: ByteArray, password: String?, alias: String?, callback: (Result<Unit>) -> Unit)
  /** Deletes the currently selected default certificate. */
  fun deleteDefaultCertificate(callback: (Result<Unit>) -> Unit)
  /** Deletes a certificate identified by its serial number. */
  fun deleteCertificateBySerialNumber(serialNumber: String, callback: (Result<Unit>) -> Unit)

  companion object {
    /** The codec used by FelectronicCertificatesHostApi. */
    val codec: MessageCodec<Any?> by lazy {
      MessagesPigeonCodec()
    }
    /** Sets up an instance of `FelectronicCertificatesHostApi` to handle messages through the `binaryMessenger`. */
    @JvmOverloads
    fun setUp(binaryMessenger: BinaryMessenger, api: FelectronicCertificatesHostApi?, messageChannelSuffix: String = "") {
      val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.felectronic_certificates_platform_interface.FelectronicCertificatesHostApi.getAllCertificates$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.getAllCertificates{ result: Result<List<DeviceCertificateMessage?>> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(MessagesPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.felectronic_certificates_platform_interface.FelectronicCertificatesHostApi.getDefaultCertificate$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.getDefaultCertificate{ result: Result<DeviceCertificateMessage?> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(MessagesPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.felectronic_certificates_platform_interface.FelectronicCertificatesHostApi.selectDefaultCertificate$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.selectDefaultCertificate{ result: Result<DeviceCertificateMessage?> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(MessagesPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.felectronic_certificates_platform_interface.FelectronicCertificatesHostApi.setDefaultCertificateBySerialNumber$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val serialNumberArg = args[0] as String
            api.setDefaultCertificateBySerialNumber(serialNumberArg) { result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesPigeonUtils.wrapError(error))
              } else {
                reply.reply(MessagesPigeonUtils.wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.felectronic_certificates_platform_interface.FelectronicCertificatesHostApi.clearDefaultCertificate$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.clearDefaultCertificate{ result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesPigeonUtils.wrapError(error))
              } else {
                reply.reply(MessagesPigeonUtils.wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.felectronic_certificates_platform_interface.FelectronicCertificatesHostApi.signWithDefaultCertificate$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val dataArg = args[0] as ByteArray
            val algorithmArg = args[1] as String
            api.signWithDefaultCertificate(dataArg, algorithmArg) { result: Result<ByteArray> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(MessagesPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.felectronic_certificates_platform_interface.FelectronicCertificatesHostApi.importCertificate$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val pkcs12DataArg = args[0] as ByteArray
            val passwordArg = args[1] as String?
            val aliasArg = args[2] as String?
            api.importCertificate(pkcs12DataArg, passwordArg, aliasArg) { result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesPigeonUtils.wrapError(error))
              } else {
                reply.reply(MessagesPigeonUtils.wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.felectronic_certificates_platform_interface.FelectronicCertificatesHostApi.deleteDefaultCertificate$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            api.deleteDefaultCertificate{ result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesPigeonUtils.wrapError(error))
              } else {
                reply.reply(MessagesPigeonUtils.wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.felectronic_certificates_platform_interface.FelectronicCertificatesHostApi.deleteCertificateBySerialNumber$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val serialNumberArg = args[0] as String
            api.deleteCertificateBySerialNumber(serialNumberArg) { result: Result<Unit> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MessagesPigeonUtils.wrapError(error))
              } else {
                reply.reply(MessagesPigeonUtils.wrapResult(null))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
    }
  }
}
